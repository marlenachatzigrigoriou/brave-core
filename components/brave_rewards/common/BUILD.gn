import("//brave/build/config.gni")

source_set("common") {
  sources = [
    "pref_names.cc",
    "pref_names.h",
    "url_constants.cc",
    "url_constants.h"
  ]

   if (!is_android) {
     sources += [
       "constants.cc",
       "constants.h"
     ]
   }
}
